services:
  demo1:
    build: .
    command: fastapi run app/main.py
    ports:
      - 8001:8000
  demo2:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - 8002:8000